#include<stdio.h>
int main()
{
	double a, b, c, d, min;
	printf("请输入四个数字:>");
	scanf("%lf%lf%lf%lf", &a, &b, &c, &d);
	//最简单代码
	min = a;
	if (b < min)
		min = b;
	if (c < min)
		min = c;
	if (d < min)
		min = d;
	printf("最小值为%lf\n", min);
}
