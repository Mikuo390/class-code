#define _CRT_SECURE_NO_WARNINGS

#include<stdio.h>
int main()
{
	int i = 0, max = 0, a[10];
	printf("请输入10个数:>");
	for (i = 0; i < 10; i++)
		scanf("%d", &a[i]);
	max = a[0];
	for (i = 1; i < 10; i++)
		if (a[i] > max)
			max = a[i];
	printf("max=%d", max);
}
